import React, { Fragment } from 'react';

// component imports

import Login from '../login/Login';

const Home = () => {
    return (
        <Fragment>
            <Login />
        </Fragment>
    );
};

export default Home;
